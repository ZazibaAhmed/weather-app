<!DOCTYPE html>
<html>
    <head>
        <link rel="stylesheet" href="/css/styles.css">
    </head>
    <body>
        {{>header}}
        <h2>{{errorMessage}}</h2>
        {{>footer}}
    </body>
</html>